import BackfieldDeck from "../deck/BackfieldDeck";

export const metadata = {
  title: "Backfield Ventures · Fund I",
  robots: { index: false, follow: false },
};

export default function Fund1Page() {
  return <BackfieldDeck />;
}
